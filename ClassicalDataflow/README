In order to see the assembly of loop.c:

clang -O -emit-llvm -c loop.c
llvm-dis loop.bc
cat loop.ll

To run the liveness and reaching-definition passes:

opt -load ./liveness.so loop.bc -o out
opt -load ./reaching-definitions.so loop.bc -o out
